import Image from "next/image";
import Link from "next/link";
import {FolderHeart,Layers3,Plus} from "lucide-react";
import {getLibraryData} from "@/lib/dashboard/data";
import {hasSupabaseEnv} from "@/lib/env";
export default async function Page(){const items=hasSupabaseEnv?await getLibraryData():[];return <div className="mx-auto max-w-6xl"><header className="flex flex-wrap items-end justify-between gap-5 border-b border-[var(--line)] pb-7"><div><p className="eyebrow">Archivo creativo</p><h1 className="editorial-title mt-2 text-5xl font-medium">Todo lo que tu marca ya dijo.</h1><p className="mt-3 max-w-2xl text-[var(--muted)]">Piezas finales, copys y versiones reunidos para reutilizar aprendizajes sin repetir ideas.</p></div><Link href="/create" className="button-primary"><Plus size={17}/>Crear pieza</Link></header>{items.length?<section className="mt-8 columns-1 gap-5 sm:columns-2 lg:columns-3">{items.map(item=><article key={item.id} className="card mb-5 break-inside-avoid overflow-hidden"><div className="relative aspect-[4/5] bg-[var(--paper)]">{item.imageUrl?<Image src={item.imageUrl} alt={item.hook} fill unoptimized className="object-cover"/>:<FolderHeart className="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 text-[var(--violet)]"/>}<span className="absolute left-3 top-3 rounded-full bg-white/90 px-2.5 py-1 text-[9px] font-black text-[var(--ink)] backdrop-blur">{item.format} · {item.status}</span></div><div className="p-5"><p className="line-clamp-2 text-lg font-black leading-snug">{item.hook}</p><p className="mt-2 line-clamp-3 text-sm leading-6 text-[var(--muted)]">{item.caption}</p><div className="mt-5 flex items-center justify-between border-t border-[var(--line)] pt-4 text-xs text-[var(--muted)]"><span className="inline-flex items-center gap-1.5"><Layers3 size={14}/>V{item.versions}</span><span>{new Date(item.createdAt).toLocaleDateString("es-PA")}</span></div></div></article>)}</section>:<section className="card mt-8 grid min-h-80 place-items-center p-8 text-center"><div><FolderHeart className="mx-auto text-[var(--violet)]" size={36}/><h2 className="editorial-title mt-5 text-3xl font-semibold">Tu archivo empieza con la primera pieza.</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[var(--muted)]">Cuando una generación termine, aparecerá aquí con su imagen, copy y versiones.</p><Link href="/create" className="button-primary mt-6">Crear contenido</Link></div></section>}</div>}
