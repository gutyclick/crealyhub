import "server-only";
import {env} from "@/lib/env";
export const instagramScopes=["instagram_business_basic","instagram_business_content_publish"];
export function redirectUri(){const uri=env.META_REDIRECT_URI??(env.NEXT_PUBLIC_APP_URL?`${env.NEXT_PUBLIC_APP_URL}/api/instagram/callback`:undefined);if(!uri)throw new Error("META_REDIRECT_URI is not configured");return uri}
export function authorizationUrl(state:string){if(!env.META_APP_ID)throw new Error("META_APP_ID is not configured");const url=new URL("https://www.instagram.com/oauth/authorize");url.search=new URLSearchParams({client_id:env.META_APP_ID,redirect_uri:redirectUri(),response_type:"code",scope:instagramScopes.join(","),state,enable_fb_login:"0",force_authentication:"1"}).toString();return url}
export async function exchangeCode(code:string){if(!env.META_APP_ID||!env.META_APP_SECRET)throw new Error("Meta credentials are not configured");const shortResponse=await fetch("https://api.instagram.com/oauth/access_token",{method:"POST",body:new URLSearchParams({client_id:env.META_APP_ID,client_secret:env.META_APP_SECRET,grant_type:"authorization_code",redirect_uri:redirectUri(),code}),signal:AbortSignal.timeout(20_000)});const short=await shortResponse.json() as {access_token?:string;user_id?:number;error_message?:string};if(!shortResponse.ok||!short.access_token)throw new Error(short.error_message??"Instagram authorization failed");const longUrl=new URL(`https://graph.instagram.com/access_token`);longUrl.search=new URLSearchParams({grant_type:"ig_exchange_token",client_secret:env.META_APP_SECRET,access_token:short.access_token}).toString();const longResponse=await fetch(longUrl,{signal:AbortSignal.timeout(20_000)});const long=await longResponse.json() as {access_token?:string;expires_in?:number;error?:{message?:string}};if(!longResponse.ok||!long.access_token)throw new Error(long.error?.message??"Could not exchange Instagram token");return{accessToken:long.access_token,userId:String(short.user_id),expiresAt:long.expires_in?new Date(Date.now()+long.expires_in*1000):null}}
export async function refreshToken(accessToken:string){const url=new URL("https://graph.instagram.com/refresh_access_token");url.search=new URLSearchParams({grant_type:"ig_refresh_token",access_token:accessToken}).toString();const response=await fetch(url,{signal:AbortSignal.timeout(20_000)});const body=await response.json() as {access_token?:string;expires_in?:number;error?:{message?:string}};if(!response.ok||!body.access_token)throw new Error(body.error?.message??"Could not refresh Instagram token");return{accessToken:body.access_token,expiresAt:body.expires_in?new Date(Date.now()+body.expires_in*1000):null}}
