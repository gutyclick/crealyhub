import {ApprovalCard} from "@/components/editorial/approval-card";
import {getApprovalQueue} from "@/lib/editorial/data";
export const dynamic="force-dynamic";
export default async function Page(){const posts=await getApprovalQueue();return <div className="mx-auto max-w-6xl"><div className="mb-8 flex items-end justify-between gap-5"><div><p className="eyebrow">Review queue</p><h1 className="editorial-title mt-2 text-4xl font-semibold sm:text-5xl">La última palabra es tuya.</h1><p className="mt-3 text-sm text-[var(--muted)]">Revisa el contenido, conserva cada versión y aprueba solo cuando esté listo.</p></div><span className="rounded-full bg-[var(--mint)] px-4 py-2 text-sm font-bold">{posts.length} pendientes</span></div>{posts.length?<div className="grid gap-6">{posts.map(post=><ApprovalCard key={post.id} post={post}/>)}</div>:<div className="card grid min-h-72 place-items-center p-8 text-center"><div><p className="text-4xl">✓</p><h2 className="editorial-title mt-3 text-3xl font-semibold">Bandeja al día.</h2><p className="mt-2 text-sm text-[var(--muted)]">El contenido generado aparecerá aquí automáticamente.</p></div></div>}</div>}
