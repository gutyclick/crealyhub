import Link from "next/link";
import {FolderHeart,Plus} from "lucide-react";
import {LibraryBoard} from "@/components/content/library-board";
import {getLibraryData} from "@/lib/dashboard/data";
import {hasSupabaseEnv} from "@/lib/env";
export const maxDuration=300;
export default async function Page(){const items=hasSupabaseEnv?await getLibraryData():[];return <div className="mx-auto max-w-6xl"><header className="flex flex-wrap items-end justify-between gap-5 border-b border-[var(--line)] pb-7"><div><p className="eyebrow">Salida editorial</p><h1 className="editorial-title mt-2 text-5xl font-medium">De aprobado a publicado.</h1><p className="mt-3 max-w-2xl text-[var(--muted)]">Revisa la pieza completa, programa el momento exacto o publícala inmediatamente.</p></div><Link href="/create" className="button-primary"><Plus size={17}/>Crear pieza</Link></header>{items.length?<div className="mt-8"><LibraryBoard items={items}/></div>:<section className="card mt-8 grid min-h-80 place-items-center p-8 text-center"><div><FolderHeart className="mx-auto text-[var(--violet)]" size={36}/><h2 className="editorial-title mt-5 text-3xl font-semibold">Tu archivo empieza con la primera pieza.</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[var(--muted)]">Cuando una generación termine, aparecerá aquí con su imagen, copy y versiones.</p><Link href="/create" className="button-primary mt-6">Crear contenido</Link></div></section>}</div>}
